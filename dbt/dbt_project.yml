name: 'fraud_detection_dbt'
version: '1.0.0'
config-version: 2

profile: 'dbt_fraud_detection'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fraud_detection_dbt:
    staging:
      +materialized: view
    marts:
      dashboard:
        +materialized: table